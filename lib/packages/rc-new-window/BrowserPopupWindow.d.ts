export declare class BrowserPopupWindow {
    private static browser;
    private static get windowBorder();
    static isSafari: boolean;
    static popupSupported: boolean;
    static popupWindowBorder: [number, number, number];
}
